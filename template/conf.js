lothar.config.merge({
    project: {
        name: '${project.name}',
        modulename: '${project.modulename}'
    },

    template: {
        suffix: '${template.suffix}'
    },

    statics: '/static'
});